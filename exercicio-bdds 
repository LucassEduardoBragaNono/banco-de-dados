exercicio

CREATE TABLE usuario(
usu_codigo PRIMARY KEY,
usu_senha CHAR(12),
usu_email VARCHAR(255) UNIQUE,
usu_nivel INT(2),
usu_nome VARCHAR(255);
);

CREATE TABLE livro(
liv_codigo PRIMARY KEY,
fk_autor VARCHAR(255) 
liv_nome VARCHAR(255),
liv_paginas INT(1000),
liv_editora VARCHAR(255);
)

CREATE TABLE carro(
  car_codigo PRIMARY KEY UNIQUE;
)
